ur-apt-dependency:
  ubuntu:
    apt:
      packages:
        - ros-humble-ur

ros2-control-dependencies:
  ubuntu:
    apt:
      packages:
        - ros-humble-ros2-control
        - ros-humble-ros2-controllers

casadi-pip-dependency:
  ubuntu:
    pip:
      packages: [casadi]

numpy-pip-dependency:
  ubuntu:
    pip:
      packages: [numpy]

pytransform3d-pip-dependency:
  ubuntu:
    pip:
      packages: [pytransform3d]